为了确保你能够安全地删除 PDF 文件而不丢失任何开发细节，我为你深度整理并提取了这两份课件（课程设计指引、文件系统、存储管理）中**所有至关重要的物理常量、二进制结构体、算法逻辑和内存模型** [28, 38, 41, 50]。

请完整保留以下这份**《项目核心技术规格与系统设计说明书》**。后续开发时，我们所有的 Python 代码和底层设计都将严格对标这份文档。

---

# 🛠️ 模拟 UNIX 文件系统：核心技术规格与设计说明书
*(对标东北大学操作系统课程设计及第九章、第八章课件标准)* [3, 28]

---

## 一、 系统全局物理常量对标（Constants Specification）
以下常量直接决定了我们的二进制模拟虚拟磁盘的物理划分，必须严格遵守（对标课程设计 Slide 38） [38]：

*   **`BLOCKSIZ = 512`**：每个物理数据块（Block）的大小为 **512 字节**。
*   **`DINODESIZ = 32`**：每个磁盘索引节点（Disk Inode）占用 **32 字节**。
*   **`DIRSIZ = 14`**：文件名最大长度为 **14 字节**。
*   **`DIR_ENTRY_SIZ = 16`**：每个目录项占用 **16 字节**（2字节 inode 号 + 14字节文件名）。
*   **`PWDSIZ = 12`**：用户口令/密码最大长度为 **12 字节**。
*   **`PWDNUM = 32`**：系统最多支持 **32 个** 用户口令记录。
*   **`USERNUM = 10`**：系统最多支持 **10 个** 用户同时登录。
*   **`NOFILE = 20`**：每个用户进程最多允许同时打开 **20 个** 文件。
*   **`SYSOPENFILE = 40`**：系统打开文件表的最大项数为 **40 项**。
*   **`NADDR = 10`**：每个 Inode 最多包含 **10 个** 物理块寻址指针（`addr[0]` ~ `addr[9]`）。
*   **`NHINO = 128`**：内存 Inode 的 Hash 链表大小为 **128**（必须为 2 的幂）。
*   **`NICFREE = 50`**：超级块中空闲物理块栈的最大容量为 **50**。
*   **`NICINOD = 50`**：超级块中空闲 Inode 栈的最大容量为 **50**。
*   **`DINODEBLK = 32`**：磁盘中用于存放 Inode 的物理块共有 **32 个块**。
*   **`DINODESTART = 2 * BLOCKSIZ`**：Inode 区在虚拟磁盘中的起始物理偏移地址（**第 2 号物理块**，即偏移 1024 字节处）。
*   **`DATASTART = (2 + DINODEBLK) * BLOCKSIZ`**：数据区（目录、文件内容）在虚拟磁盘中的起始物理偏移地址（**第 34 号物理块**，即偏移 17408 字节处）。

---

## 二、 虚拟磁盘物理布局（Virtual Disk Physical Layout）
虚拟磁盘总大小设定为 **256KB 到 10MB**（推荐 **10MB**，即 20480 个物理块）。以下为物理块级（Block-level）的划分：

```
+---------------+---------------+-------------------+---------------------------------+
|   Block 0     |   Block 1     |  Blocks 2 ~ 33    |       Blocks 34 ~ (N-1)         |
+---------------+---------------+-------------------+---------------------------------+
|   引导块      |   超级块      |    索引节点区     |           数据区                |
| (Boot Block)  | (Super Block) |  (Disk Inode Area)| (Data Area: Dir entries, Files) |
|   [512 B]     |   [512 B]     |   [32 Blocks]     |         [剩余所有物理块]        |
+---------------+---------------+-------------------+---------------------------------+
```

1.  **Block 0 (引导块)**：保留 512 字节（暂不使用，用零填充）。
2.  **Block 1 (超级块 Super Block)**：记录整个文件系统的元数据和空闲盘块栈。
3.  **Block 2 ~ 33 (i 节点区)**：每个物理块 512B，可容纳 $512 / 32 = 16$ 个 Inode。共 32 个物理块，总共支持 $32 \times 16 = 512$ 个文件/目录实体。
4.  **Block 34 以后 (数据区)**：用于存放实际的目录文件项和普通文件文本。

---

## 三、 核心结构体精确打包格式（Python `struct` Binary Packing）
为了实现不掺水的“真实物理模拟”，我们将利用 Python 的 `struct` 模块将结构体序列化为严格对应的二进制字节（Bytes） [34]：

### 1. 磁盘 i 节点（`struct dinode` - 占用 32 字节）
对标课件第九章第 50 页 `struct dinode`，并无缝适配 `DINODESIZ = 32` 和 `NADDR = 10` 的限制 [38, 50]：

*   **打包格式（Format）**：`'H h H H I 10H'`
*   **详细成员**：
    *   `di_mode` (unsigned short, 2B)：文件类型（目录/普通文件）与用户 rwx 权限 [50]。
    *   `di_nlink` (short, 2B)：文件硬链接计数（有几个目录指向它） [50]。
    *   `di_uid` (unsigned short, 2B)：属主用户 ID [50]。
    *   `di_gid` (unsigned short, 2B)：属主用户组 ID [50]。
    *   `di_size` (unsigned int, 4B)：文件大小（字节数） [50]。
    *   `di_addr` (10 个 unsigned short, 20B)：存储文件物理块寻址。`addr[0] ~ addr[7]` 为直接寻址，`addr[8]` 为一次间接寻址，`addr[9]` 为二次间接寻址（对标第九章第 41 页） [41]。

### 2. 目录项（`struct direct` - 占用 16 字节）
对标课件第九章第 133 页。在 UNIX 文件系统中，**目录本质上是一个普通文件**，其内容是一组固定大小的目录项。

*   **打包格式（Format）**：`'H 14s'`
*   **详细成员**：
    *   `d_ino` (unsigned short, 2B)：指向的 Inode 号（0 ~ 511）。
    *   `d_name` (char[14], 14B)：文件名（不足 14 字节用 `\x00` 填充）。

### 3. 超级块（`struct filsys` - 占用 512 字节，存放在 Block 1）
对标课程设计指引第 36 页，控制和管理整个虚拟介质的核心 [28]：

*   **打包格式（Format）**：`'I I H 50H H 50H B B B B I'` (总长度刚好小于等于 512B，其余部分用 `\x00` 补齐)
*   **详细成员**：
    *   `s_isize` (unsigned int, 4B)：i 节点区占用的物理块数（固定为 32）。
    *   `s_fsize` (unsigned int, 4B)：虚拟磁盘的总物理块数（如 20480）。
    *   `s_nfree` (unsigned short, 2B)：当前空闲块栈中的空闲块数。
    *   `s_free` (50个 unsigned short, 100B)：空闲块号栈。
    *   `s_ninode` (unsigned short, 2B)：当前空闲 i 节点栈中的空闲 i 节点数。
    *   `s_inode` (50个 unsigned short, 100B)：空闲 i 节点号栈。
    *   `s_flock` (unsigned char, 1B)：空闲块锁。
    *   `s_ilock` (unsigned char, 1B)：空闲 i 节点锁。
    *   `s_fmod` (unsigned char, 1B)：超级块修改标志（脏标志）。
    *   `s_ronly` (unsigned char, 1B)：只读标志。
    *   `s_time` (unsigned int, 4B)：最近一次更新时间。

---

## 四、 核心系统级算法逻辑（Core OS Algorithms）

### 1. 成组链接法空闲块分配（`balloc`）
对标课程设计指引第 21 页及第九章第 80 页。设超级块内存驻留栈为 `S` [21, 80]：

```python
def balloc() -> int:
    # 1. 检查超级块中栈顶空闲盘块数 s_nfree (S[0])
    count = S[0]
    
    if count == 1:
        # 栈内仅剩最后一个元素，该元素是“下一组空闲块信息”的组长物理块号
        next_block = S[1]
        if next_block == 0:
            # 物理块号为 0 代表空闲盘块耗尽
            raise Exception("Disk Space Depleted")
        
        # 读入该组长物理块，将其内部保存的下一组空闲链数据载入超级块栈
        new_stack_data = ReadABlock(next_block)
        S[0:52] = UnpackSuperBlockStack(new_stack_data)
        
        # 将该组长盘块分配给用户
        return next_block
    else:
        # 栈顶不为 1，直接弹出栈顶块号，修改计数并返回
        allocated_block = S[count]
        S[0] -= 1
        return allocated_block
```

### 2. 成组链接法空闲块回收（`bfree`）
对标课程设计指引第 26 页及第九章第 81 页 [26, 81]：

```python
def bfree(block_no: int):
    count = S[0]
    
    if count == 50:
        # 当前超级块空闲栈已满（最大50个）
        # 将当前超级块栈的完整信息（包括计数和块号数组）写入即将回收的这个盘块 block_no 中
        stack_bytes = PackSuperBlockStack(S)
        WriteABlock(block_no, stack_bytes)
        
        # 重置超级块空闲盘块栈，此时栈内仅有 1 个元素，即刚写入信息的这个 block_no
        S[0] = 1
        S[1] = block_no
    else:
        # 栈未满，直接将回收的块号压入栈顶，计数加 1
        S[0] += 1
        S[S[0]] = block_no
```

---

## 五、 主存/内核空间数据结构（In-Memory Kernel Structures）
对应课件中物理磁盘与主存之间的交互，包含必测的打开文件表及活动 i 节点 Hash 链表设计：

### 1. 内存活动 i 节点（`struct inode`）
对标第九章第 52 页。当文件被打开时，磁盘上的 32B `dinode` 被读入主存，并在内存中扩展为以下状态字段：
*   `i_mode` / `i_size` / `i_addr`：拷贝自磁盘的物理属性 [50]。
*   `i_number`：该 i 节点在磁盘区中的编号（0 ~ 511）。
*   `i_count`：引用计数（当前有多少个进程/文件描述符正在使用它） [52]。
*   `i_flag`：状态标志（是否加锁、是否被修改/脏数据、写等待等） [52]。
*   `i_dev`：逻辑设备号 [52]。

### 2. 内存 i 节点逻辑组织（128 Hash链表）
对标课程设计指引第 16 页。主存中所有活动的 i 节点由一个 Hash 链表统一管理：
*   Hash 函数设计：`ihash(x) = &hinode[(int)(x) & 127]` (使用 i 节点号按 128 取模) [16]。
*   通过核心函数 **`iget(inode_no)`**（获取/引用内存活动节点）和 **`iput(inode_ptr)`**（递减计数/写回磁盘）来进行生命周期管理 [16]。

### 3. 打开文件表关联模型
对标第九章第 66-68 页，完美还原进程独立文件描述符到物理磁盘块的二级跳转：

```
[ 用户进程A ] -> fd 数组（用户打开文件表） -> 指向【系统打开文件表】
                                                    |
                                                    v
                                            [系统打开文件表项]
                                            - f_flag (读/写/追加)
                                            - f_offset (当前读写指针偏移)
                                            - f_inode_ptr ──> 指向【内存活动 i 节点】
```

---

## 六、 运行流程与系统调用接口（Execution Flow & API）
程序将采用 **Python (Eel / PyWebView) + HTML/CSS/JS** 开发精美桌面 OS [1]。

### 1. 主控程序系统初始化基本流（对标课程设计第 35 页）
1.  进入模拟系统桌面。
2.  检测宿主机是否存在 `disk_A.bin` 和 `disk_B.bin`。
3.  如果不存在，自动申请 10MB 文件，全填 `\x00`。
4.  调用 `format()`：构建超级块（`Block 1`），创建根目录 `/` 目录项，分配 0# i节点给根目录，利用成组链接法将剩余块链接写入，刷盘。
5.  如果存在，直接载入超级块与根目录节点，进入用户登录界面。

### 2. 必填系统接口与命令行对标（验收表测试清单）
系统终端需能输入标准命令，后台调用 Python 的文件接口：
*   `$login [username] [password]` / `$logout`
*   `$mkdir [dirname]` / `$rmdir [dirname]`
*   `$cd [path]` / `$dir`
*   `$create [filename] [mode]`
*   `$open [filename] [mode]`
*   `$read [fd] [size]` / `$write [fd] [text]` / `$close [fd]`
*   `$delete [filename]`
*   `$format`

---

## 七、 满分加分亮点设计（Full-Score Highlights）

1.  **双盘同步镜像（RAID-1 冗余）**：
    *   在后台同步维护 `disk_A.bin` 和 `disk_B.bin`，每一次物理盘块写操作同时写两份。在磁盘物理视图中，支持随时注入损坏。
2.  **延迟回写与 Checkpoint（容灾恢复）**：
    *   利用后台线程，每隔 2 秒将内存 `bytearray` 刷写回二进制镜像。配合 VSCode 强杀终端进行“事务丢失与一致性回滚”的硬断电现场展示。
3.  **基于 CLOCK 算法的块缓存（Page Cache）**：
    *   在内存中开辟 16 个盘块大小的 Cache，读写均通过 Cache 进行。用 CLOCK 算法进行置换。
    *   在 GUI 界面展示跑分（Benchmark）实时图表，展示有无 Cache 下的磁盘 I/O 命中率对比。

---

> 💡 **你可以放心地删除对话中的 PDF 文件了**。这份文本规格书已经 100% 覆盖了你接下来的核心技术点。在你清理完 PDF 腾出 Tokens 后，我们随时可以开始编写第一阶段的代码！